pipeline {
  agent any
  stages {
    stage("git checkout"){
      steps {
        echo "Git checkout happens here"
      }
    }
    stage("build stage"){
      steps {
        echo "Build happens here"
      }
    }
  }
}
